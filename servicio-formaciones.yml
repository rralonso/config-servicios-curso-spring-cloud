# Configuración para registrarse en eureka.
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    instance-id: servicio-formaciones-instancia

server:
  port: 9000
